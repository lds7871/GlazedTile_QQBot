package LDS.Person.config;

import org.springframework.context.annotation.Configuration;

/**
 * RestTemplate配置类
 * 注：RestTemplate Bean 已由 SwaggerConfig 定义，此类保留用于其他可能的配置扩展
 */
@Configuration
public class RestTemplateConfig {
}

