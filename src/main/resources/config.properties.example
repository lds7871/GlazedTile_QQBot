#注：除这些以外，你还需要配置DEEPSEEK_API_KEY于用户环境变量

#========== 代理配置 ==========
# 是否启用代理（true/false）
proxy.is.open=false
# 代理主机地址
proxy.host=127.0.0.1
# 代理端口 https
proxy.port=33210
# 代理端口 socks
proxy.port.socks=33211

#========== QQBot配置 ==========
# 是否启用 NapCat（true/false）我的天我为什么要写个这玩意
NapCatIsOpen=true

# NapCat HTTP API 基础 URL（强烈建议本地服务）
NapCatApiBase=http://0.0.0.0:3000

# NapCat HTTP 认证令牌（从 NapCat 配置中获取）
NapCatAuthToken=YOUR_NAPCAT_AUTH_TOKEN

# QQ 机器人 ID（Napcat 运行的 QQ 号）
NapcatQQID=YOUR_QQ_BOT_ID

#========== WebSocket配置 ==========
# 本地 Spring 应用的 WebSocket 端点(本地连接测试，实际运行未使用)
WS_URL_LOCAL=ws://localhost:7090/onebot

#  NapCat 服务器的 WebSocket 端点 本地端点就填写Websocket服务器配置上的URL（注意包含 access_token 参数）
WS_URL_REMOTE=ws://YOUR_SERVER_IP:3001?access_token=PASSWORD

# 这个就这样不用改动
WS_URL_SELECT=LOCAL

# WebSocket 认证令牌(用于本地连接)
WS_TOKEN=YOUR_WEBSOCKET_TOKEN
